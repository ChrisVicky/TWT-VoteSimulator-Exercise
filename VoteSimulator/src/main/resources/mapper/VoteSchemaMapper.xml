<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="TWT.Homework.VoteSimulator.dao.VoteSimulatorMapper">
    <select id="getAllQuestion" resultType="QuestionSchema"> SELECT * FROM question; </select>
    <select id="getAllChoice" resultType="ChoiceSchema"> SELECT * FROM choice; </select>
    <select id="getAllVoteId" resultType="int"> SELECT question.vote_id FROM question WHERE user_id = #{useId}; </select>
    <select id="getIndividualQuestion" resultType="QuestionSchema"> SELECT * FROM question WHERE vote_id = #{voteId}; </select>
    <select id="getIndividualVoteChoice" resultType="ChoiceSchema"> SELECT * FROM choice WHERE vote_id = #{voteId}; </select>
    <select id="getVoteIdThroughQuestion" resultType="int"> SELECT vote_id FROM question WHERE question = #{question} AND user_id = #{userId}; </select>
    <select id="getUserIdThroughVoteId" resultType="int"> SELECT user_id FROM question WHERE question.vote_id = #{voteId}; </select>


    <insert id="addQuestion"> INSERT INTO question (question, user_id) VALUES (#{question}, #{userId}); </insert>
    <insert id="addChoice"> INSERT INTO choice (choice, choice_id, vote_id, times) VALUES (#{choice}, #{choiceId}, #{voteId}, 0); </insert>

    <delete id="deleteQuestion"> DELETE FROM question WHERE question.vote_id=#{voteId}; </delete>
    <delete id="deleteChoice"> DELETE FROM choice WHERE choice.vote_id=#{voteId}; </delete>
</mapper>
